
export const Valid_Pass = new RegExp('(?=.*[A-Z])(?=.*[!@#$&*])(?=.*[0-9])(?=.*[a-z]).{8,}$');

export const Valid_PostalCode = new RegExp('[0-9]{10,11}');

export const Valid_eq = new RegExp('[0-9]{12,13}');

export const Valid_maliat = new RegExp('[0-9]{10,11}');

export const Valid_Number = new RegExp('^[0][9][0-9]{8}');

export const Valid_MeliCode = new RegExp('[0-9]{10}');

export const Confirm_Code = new RegExp('[0-9]{5}');